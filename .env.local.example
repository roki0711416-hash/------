# ローカルデモ用（commit可）
# 1) cp .env.local.example .env.development.local
# 2) 値を埋める

# Postgres（Neon / Vercel Postgres など）
# 例: postgres://USER:PASSWORD@HOST:5432/DB?sslmode=require
DATABASE_URL=

# Stripe
STRIPE_SECRET_KEY=
# Payment Element（クライアント側）で使う公開鍵: pk_test_... / pk_live_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# AdSense (global mobile unit)
# Set to the Ad Unit "slot" id (numbers only).
NEXT_PUBLIC_ADSENSE_GLOBAL_SLOT=
# 年額6,800円（税込）プランの Price ID（例: price_...）
STRIPE_PRICE_ID_YEARLY=
# 月額680円（税込）プランの Price ID（例: price_...）
STRIPE_PRICE_ID_MONTHLY=
# 互換用（未設定なら STRIPE_PRICE_ID_MONTHLY を参照）。既存の月額IDをここに入れてもOK
STRIPE_PRICE_ID=
# `stripe listen --print-secret` で出る whsec_...
STRIPE_WEBHOOK_SECRET=

# ローカルでは http://localhost:3000 を推奨
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# デモ用: 1 で Premium 扱いに強制（Stripe/DB反映を待たずにUI確認したいとき）
# ※Stripeのテスト動作確認をしたい場合は未設定（空）か 0 にしてください。
SLOKASU_PREMIUM_PREVIEW=
